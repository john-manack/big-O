// illustrates Rule 3: Different terms for inputs

function compressBoxesTwice(boxes, boxes2) {
    boxes.forEach(boxes => console.log(boxes))

    boxes2.forEach(boxes => console.log(boxes))
}

// Big-O = O(a + b)